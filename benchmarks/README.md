# Benchmarks

Benchmark commands and result artifacts will be added after the distributed baseline is implemented. Numbers must be generated by code and recorded with machine and configuration metadata.
