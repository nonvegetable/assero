# Network package

The package currently provides a compact message size model, an in-memory virtual transport with queued delivery for offline nodes, and incremental replica synchronization. It is suitable for deterministic tests and simulation; persistent TCP transport and protobuf benchmarking remain follow-on work.
